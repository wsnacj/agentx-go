// Package resume provides portable continuation readback, Host wake dispatch,
// and bounded scheduler service coordination.
//
// The package owns no system process, concrete durable backend, credential,
// provider, Runner, Scene, authorization policy, or production side effect.
package resume
