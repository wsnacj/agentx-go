// Package protocol defines versioned, provider-neutral AgentX runtime facts.
//
// It owns wire shapes, normalization, and validation only. It is not an
// authorization source, durable store, projection owner, or runtime backend.
package protocol
